public String nameFromEmail(String email) {
    String firstName = email.split('.')[0].toLowerCase();
    String lastName = email.split('.')[1].toLowerCase();

    return firstName.capitalize() + lastName.capitalize();
}